% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survkit.R
\name{survkit}
\alias{survkit}
\title{survkit: Survival Curves with Risk Tables}
\usage{
survkit(
  data,
  time,
  status,
  group,
  adjust_for = NULL,
  reference = NULL,
  viridis_option = "viridis",
  conf_int = TRUE,
  conf_int_alpha = 0.2,
  censor_marks = TRUE,
  censor_shape = 3,
  censor_size = 3,
  censor_stroke = 1.5,
  line_width = 1.2,
  risk_table = TRUE,
  risk_table_height = 0.25,
  risk_table_text_size = 4,
  time_breaks = NULL,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{data}{Data frame containing survival data}

\item{time}{Column name for time-to-event variable (string)}

\item{status}{Column name for event indicator (1 = event, 0 = censored)}

\item{group}{Column name for grouping variable}

\item{adjust_for}{Optional character vector of covariate names for Cox adjustment}

\item{reference}{Optional named list of reference values for covariates (default: mean/mode)}

\item{viridis_option}{Viridis palette: "viridis" (default), "magma", "inferno", "plasma", "cividis"}

\item{conf_int}{Show confidence intervals (default TRUE)}

\item{conf_int_alpha}{Transparency for confidence interval ribbons (default 0.2)}

\item{censor_marks}{Show censoring marks (default TRUE, KM mode only)}

\item{censor_shape}{Shape for censor marks (default 3 = plus sign, see ?pch for options)}

\item{censor_size}{Size for censor marks (default 3)}

\item{censor_stroke}{Stroke width for censor marks (default 1.5)}

\item{line_width}{Width of survival curves (default 1.2)}

\item{risk_table}{Include risk table (default TRUE)}

\item{risk_table_height}{Proportion of plot height for risk table (default 0.25)}

\item{risk_table_text_size}{Text size in risk table (default 4)}

\item{time_breaks}{Numeric vector of time points for axis and risk table (auto-generated if NULL)}

\item{title}{Optional plot title}

\item{subtitle}{Optional plot subtitle}
}
\value{
A patchwork object combining survival curve and risk table
}
\description{
Generate publication-ready survival curves with integrated risk tables.
Uses viridis colors, golden ratio dimensions, and 600 DPI optimization.
}
\examples{
\dontrun{
library(survival)

# Basic KM curve with defaults
survkit(lung, "time", "status", "sex")

# Adjusted for covariates
survkit(lung, "time", "status", "sex",
        adjust_for = c("age", "ph.ecog"))

# Custom styling
survkit(lung, "time", "status", "sex",
        line_width = 2,
        censor_size = 4,
        conf_int_alpha = 0.3,
        viridis_option = "plasma")

# Minimal style - thin lines, no CI
survkit(lung, "time", "status", "sex",
        line_width = 0.8,
        conf_int = FALSE)
}
}
